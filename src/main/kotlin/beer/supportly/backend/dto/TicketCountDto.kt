package beer.supportly.backend.dto

/**
 * Data transfer object for ticket count
 *
 * @property count the number of tickets
 *
 * @constructor constructor with all values
 */
data class TicketCountDto(
    val count: Long
)